# ~~ Generated by projen. To modify, edit .projenrc.ts and run "projen".

name: build
on:
  pull_request: {}
  workflow_dispatch: {}
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    outputs:
      self_mutation_happened: ${{ steps.self_mutation.outputs.self_mutation_happened }}
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Install projen
        run: npm install --global --quiet --no-progress projen
      - name: Setup pnpm
        uses: pnpm/action-setup@v2.2.4
        with:
          version: 8.9.0
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18.17.0
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: us-east-2
          role-to-assume: arn:aws:iam::848930331875:role/SL-GitHubActions-OIDC-NpmRegistryAccess
          role-duration-seconds: 900
      - name: AWS CodeArtifact Login
        run: pnpm run ca:login
      - name: Install dependencies
        run: pnpm i --frozen-lockfile
      - name: build
        run: projen build
      - name: Find mutations
        id: self_mutation
        run: |-
          git add .
          git diff --staged --patch --exit-code > .repo.patch || echo "self_mutation_happened=true" >> $GITHUB_OUTPUT
      - name: Upload patch
        if: steps.self_mutation.outputs.self_mutation_happened
        uses: actions/upload-artifact@v3
        with:
          name: .repo.patch
          path: .repo.patch
      - name: Fail build on mutation
        if: steps.self_mutation.outputs.self_mutation_happened
        run: |-
          echo "::error::Files were changed during build (see build log). If this was triggered from a fork, you will need to update your branch."
          cat .repo.patch
          exit 1
      - name: Backup artifact permissions
        run: cd dist && getfacl -R . > permissions-backup.acl
        continue-on-error: true
      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: build-artifact
          path: dist
  build-workspace:
    name: Build Workspace
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    if: "! needs.build.outputs.self_mutation_happened"
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v3
        with:
          name: build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Install projen
        run: npm install --global --quiet --no-progress projen
      - name: Setup pnpm
        uses: pnpm/action-setup@v2.2.4
        with:
          version: 8.9.0
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18.17.0
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: us-east-2
          role-to-assume: arn:aws:iam::848930331875:role/SL-GitHubActions-OIDC-NpmRegistryAccess
          role-duration-seconds: 900
      - name: AWS CodeArtifact Login
        run: pnpm run ca:login
      - name: Install dependencies
        run: pnpm i --frozen-lockfile
      - name: Build Workspace
        run: projen ws:build --reporter-hide-prefix
